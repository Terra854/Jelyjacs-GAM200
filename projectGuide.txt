(List your different project settings, different configuration setup (if any),
differences in different projects under the same solution (if you have more than
one), how to run editor mode vs only game mode.)